# Initialise the Robot
(put this at the start of your code before typing anything)
(remember the robot uses python)


import cv2
import time
from xgolib import XGO
from xgoedu import XGOEDU
XGO_mini = XGO("xgomini")
XGO_edu = XGOEDU()
